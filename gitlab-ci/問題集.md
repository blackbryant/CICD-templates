### linux 權限問題: 將docker加入到使用者devuser

將docker加入到使用者devuser

```bash
sudo groupadd docker
sudo usermod -aG docker devuser
newgrp docker
sudo systemctl restart docker

```
測試:
 * su - devuser
 * docker ps


#### linux 安裝 docker-compose

```bash
## 移除舊版本
sudo apt-get remove docker docker-engine docker.io containerd runc

## 安裝必要套件
sudo apt-get update
sudo apt-get install \
    ca-certificates \
    curl \
    gnupg \
    lsb-release

## 新增 Docker 官方 GPG 金鑰
# 建立存放金鑰的目錄
sudo mkdir -p /etc/apt/keyrings
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /etc/apt/keyrings/docker.gpg

## 設定存儲庫 (Repository)
echo \
  "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/ubuntu \
  $(lsb_release -cs) stable" | sudo tee /etc/apt/sources.list.d/docker.list > /dev/null

## 安裝 Docker Engine
sudo apt-get update
sudo apt-get install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin

```
測試:sudo docker run hello-world

#### 抓取registry使用http方式

* vi /etc/docker/daemon.json


```bash
{
  "insecure-registries": ["registry.gitlab.test.com.tw:8929"]
}

```
* 重啟 Docker 服務
sudo systemctl restart docker

#### 設定SMTP的地址

```bash

gitlab_rails['smtp_enable'] = true
gitlab_rails['smtp_address'] = "smtp.gmail.com"
gitlab_rails['smtp_port'] = 465
gitlab_rails['smtp_user_name'] = "e49123434e@gmail.com"
gitlab_rails['smtp_password'] = "1234567890123456"
gitlab_rails['smtp_domain'] = "smtp.gmail.com"
gitlab_rails['smtp_authentication'] = "login"
gitlab_rails['smtp_enable_starttls_auto'] = false
gitlab_rails['smtp_tls'] = true
gitlab_rails['smtp_openssl_verify_mode'] = 'peer'
gitlab_rails['gitlab_email_enabled'] = true
gitlab_rails['gitlab_email_from'] = 'e49123434e@gmail.com'
gitlab_rails['gitlab_email_display_name'] = 'gitlab platform'
gitlab_rails['gitlab_email_reply_to'] = 'e49123434e@gmail.com'


```






